import 'package:flutter/material.dart';

// TODO: Inicializar Supabase aquí antes de runApp()
// await Supabase.initialize(url: ..., anonKey: ...);
// Las credenciales vienen de variables de entorno (--dart-define), nunca hardcodeadas.

// TODO: Registrar providers de Provider (MultiProvider) aquí.

void main() {
  // Placeholder — sin lógica todavía.
  runApp(const MaterialApp(home: Scaffold(body: Center(child: Text('Tecnico Inventario')))));
}
